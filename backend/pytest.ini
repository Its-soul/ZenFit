[pytest]
addopts = -m "not integration"
markers =
    integration: requires running external infrastructure
    model: requires downloaded local model weights
    external_api: requires a configured external API credential
    dataset: requires downloaded datasets
    load: load and performance tests
    device_manual: requires a physical browser/camera device
    slow: may take more than a few seconds
