# Open-set evidence layout

Keep legally usable evaluation images outside Git under `private/supported_food`, `private/unknown_food`, and `private/non_food`. Copy `manifest.example.json`, record provenance/license/category for every item, and never store secrets or private user meal images without explicit consent.
