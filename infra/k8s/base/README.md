# Kubernetes Base

Kubernetes support is prepared but not required for local development.

The current project runs locally with Docker Compose. Add deployment manifests here when the app is ready for staging.

